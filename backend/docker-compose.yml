version: '3.8'

services:
  # MongoDB Database
  mongodb:
    image: mongo:7
    container_name: cognistock-mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: cognistock
      MONGO_INITDB_ROOT_PASSWORD: cognistock_password
      MONGO_INITDB_DATABASE: cognistock
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
      - mongodb_config:/data/configdb
    networks:
      - cognistock-network
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: cognistock-redis
    restart: unless-stopped
    command: redis-server --requirepass cognistock_redis_password
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - cognistock-network
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  # RabbitMQ Message Queue
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: cognistock-rabbitmq
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: cognistock
      RABBITMQ_DEFAULT_PASS: cognistock_rabbitmq_password
      RABBITMQ_DEFAULT_VHOST: cognistock
    ports:
      - "5672:5672"   # AMQP port
      - "15672:15672" # Management UI
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
      - rabbitmq_logs:/var/log/rabbitmq
    networks:
      - cognistock-network
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5

  # MongoDB Express (Optional - Database Management UI)
  mongo-express:
    image: mongo-express:latest
    container_name: cognistock-mongo-express
    restart: unless-stopped
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: cognistock
      ME_CONFIG_MONGODB_ADMINPASSWORD: cognistock_password
      ME_CONFIG_MONGODB_URL: mongodb://cognistock:cognistock_password@mongodb:27017/
      ME_CONFIG_BASICAUTH_USERNAME: admin
      ME_CONFIG_BASICAUTH_PASSWORD: admin123
    ports:
      - "8081:8081"
    depends_on:
      - mongodb
    networks:
      - cognistock-network

networks:
  cognistock-network:
    driver: bridge

volumes:
  mongodb_data:
    driver: local
  mongodb_config:
    driver: local
  redis_data:
    driver: local
  rabbitmq_data:
    driver: local
  rabbitmq_logs:
    driver: local
